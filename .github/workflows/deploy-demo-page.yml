name: Deploy Demo Page

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    uses: marcintk/ha-shared/.github/workflows/deploy-demo-page.yml@main
    permissions:
      contents: read
      pages: write
      id-token: write
