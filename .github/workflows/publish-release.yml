name: Publish Release

on:
  push:
    tags:
      - 'v*.*.*'
      - 'v*.*.*-*'

jobs:
  release:
    uses: marcintk/ha-card-shared/.github/workflows/shared-publish-release.yml@v2.1.0
    permissions:
      contents: write
